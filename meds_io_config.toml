# MEDS per-variable output overrides -- GENERATED by `meds_main --dump-io-config`.
#
# Point the main config at this file with [output].io_config = "meds_io_config.toml".
# Entries here are the FINEST granularity and OVERRIDE the axis / group / per-tier
# switches in the main config.
#
#   name = true      -> force ON at the variable's registry-default streams
#   name = false     -> force OFF everywhere
#   name = "F D M Y" -> replace the stream mask (F fast, D daily, M monthly, Y annual;
#                       Y is forbidden on a cohort- or patch-dimensioned variable)
#
# A name that matches no registered variable is a hard error, not a silent no-op.
# Every line below is commented out, so this file as generated changes nothing.

[variables]
# nplant_cohort = "D M"                               # [structure, cohort] plant number density (plant/m2)
# dbh_cohort = "D M"                                  # [structure, cohort] diameter at breast height (cm)
# height_cohort = "D M"                               # [structure, cohort] height (m)
# basal_area_cohort = "M"                             # [structure, cohort] basal area per plant (cm2/plant)
# agb_cohort = "D M"                                  # [structure, cohort] aboveground biomass per plant (kgC/plant)
# leaf_area_cohort = "M"                              # [structure, cohort] leaf area per plant (m2/plant)
# lai_cohort = "M"                                    # [structure, cohort] cohort leaf area index (m2/m2)
# growth_avg_cohort = "M"                             # [structure, cohort] moving-average growth (cm/yr)
# overtopping_lai_cohort = "M"                        # [structure, cohort] cumulative LAI of taller cohorts (m2/m2)
# pft_cohort = "D M"                                  # [structure, cohort] plant functional type index (-)
# owner_patch = "D M"                                 # [structure, cohort] owning patch (1-based) (-)
# global_cohort_id = "D M"                            # [structure, cohort] persistent cohort id (-)
# leaf_carbon_cohort = "M"                            # [structure, cohort] leaf carbon per plant (kgC/plant)
# fineroot_carbon_cohort = "M"                        # [structure, cohort] fine-root carbon per plant (kgC/plant)
# wood_carbon_cohort = "M"                            # [structure, cohort] wood carbon per plant (kgC/plant)
# storage_carbon_cohort = "M"                         # [structure, cohort] non-structural carbon per plant (kgC/plant)
# sla_cohort = "M"                                    # [structure, cohort] specific leaf area (m2/kgC)
# vcmax25_cohort = "M"                                # [structure, cohort] max carboxylation rate at 25 degC (umol/m2/s)
# rd25_cohort = "M"                                   # [structure, cohort] leaf dark respiration at 25 degC (umol/m2/s)
# llspan_cohort = "M"                                 # [structure, cohort] leaf lifespan (yr)
# pheno_flush_cohort = "M"                            # [structure, cohort] phenology flush governor (-)
# pheno_shed_cohort = "M"                             # [structure, cohort] phenology active-shed governor (-)
# dmax_psi_leaf_cohort = "D M"                        # [structure, cohort] predawn (daily-max) leaf water potential (MPa)
# dbh_growth_cohort = "M"                             # [structure, cohort] diameter growth rate (cm/yr)
# agb_growth_cohort = "M"                             # [structure, cohort] per-plant AGB growth rate (kgC/plant/yr)
# mort_rate_cohort = "M"                              # [structure, cohort] mortality hazard rate (1/yr)
# area_patch = "D M"                                  # [structure, patch] patch area fraction (-)
# age_patch = "M"                                     # [structure, patch] time since disturbance (yr)
# dist_type_patch = "M"                               # [structure, patch] disturbance type (-)
# cohort_offset = "D M"                               # [structure, patch] first cohort of patch (CSR) (-)
# cohort_count = "D M"                                # [structure, patch] cohorts in patch (CSR) (-)
# global_patch_id = "D M"                             # [structure, patch] persistent patch id (-)
# nplant_patch = "D M"                                # [structure, patch] patch plant number density (plant/m2)
# lai_patch = "D M"                                   # [structure, patch] patch leaf area index (m2/m2)
# agb_patch = "D M"                                   # [structure, patch] patch aboveground biomass (kgC/m2)
# basal_area_patch = "M"                              # [structure, patch] patch basal area (m2/m2)
# nplant_site = "D M Y"                               # [structure, site] site total plant number (plant/m2)
# basal_area_site = "D M Y"                           # [structure, site] site total basal area (m2/m2)
# agb_site = "D M Y"                                  # [structure, site] site aboveground biomass (kgC/m2)
# lai_site = "D M Y"                                  # [structure, site] site leaf area index (m2/m2)
# mean_dbh_site = "D M Y"                             # [structure, site] basal-area-weighted mean DBH (cm)
# mean_height_site = "D M Y"                          # [structure, site] basal-area-weighted mean height (m)
# canopy_height_site = "D M Y"                        # [structure, site] height of the tallest cohort (m)
# n_cohort_site = "D M Y"                             # [structure, site] live cohort count (-)
# n_patch_site = "D M Y"                              # [structure, site] live patch count (-)
# nplant_pft = "M Y"                                  # [structure, pft] plant number density by PFT (plant/m2)
# agb_pft = "M Y"                                     # [structure, pft] aboveground biomass by PFT (kgC/m2)
# lai_pft = "M Y"                                     # [structure, pft] leaf area index by PFT (m2/m2)
# basal_area_pft = "M Y"                              # [structure, pft] basal area by PFT (m2/m2)
# mean_dbh_pft = "M Y"                                # [structure, pft] basal-area-weighted mean DBH by PFT (cm)
# nplant_size = "M Y"                                 # [structure, dbh_class] plant number density by DBH class (plant/m2)
# agb_size = "M Y"                                    # [structure, dbh_class] aboveground biomass by DBH class (kgC/m2)
# lai_size = "M Y"                                    # [structure, dbh_class] leaf area index by DBH class (m2/m2)
# basal_area_size = "M Y"                             # [structure, dbh_class] basal area by DBH class (m2/m2)
# agb_growth_size = "M Y"                             # [structure, dbh_class] AGB growth rate by DBH class (kgC/m2/yr)
# mort_rate_size = "M Y"                              # [structure, dbh_class] nplant-weighted mortality rate by DBH class (1/yr)
# gpp_site = "D M Y"                                  # [carbon, site] site gross primary productivity (period total) (kgC/m2)
# npp_site = "D M Y"                                  # [carbon, site] site net primary productivity (period total) (kgC/m2)
# leaf_resp_site = "D M Y"                            # [carbon, site] site leaf dark respiration (period total) (kgC/m2)
# stem_resp_site = "D M Y"                            # [carbon, site] site stem maintenance respiration (period total) (kgC/m2)
# root_resp_site = "D M Y"                            # [carbon, site] site fine-root maintenance respiration (period total) (kgC/m2)
# leaf_carbon_site = "D M Y"                          # [carbon, site] site leaf carbon (kgC/m2)
# fineroot_carbon_site = "D M Y"                      # [carbon, site] site fine-root carbon (kgC/m2)
# wood_carbon_site = "D M Y"                          # [carbon, site] site wood carbon (kgC/m2)
# storage_carbon_site = "D M Y"                       # [carbon, site] site non-structural carbon (kgC/m2)
# bgb_site = "D M Y"                                  # [carbon, site] site belowground biomass (kgC/m2)
# veg_carbon_site = "D M Y"                           # [carbon, site] site total live vegetation carbon (kgC/m2)
# gpp_pft = "M Y"                                     # [carbon, pft] gross primary productivity by PFT (period total) (kgC/m2)
# npp_pft = "M Y"                                     # [carbon, pft] net primary productivity by PFT (period total) (kgC/m2)
# gpp_patch = "D M"                                   # [carbon, patch] patch gross primary productivity (period total) (kgC/m2)
# npp_patch = "D M"                                   # [carbon, patch] patch net primary productivity (period total) (kgC/m2)
# gpp_cohort = "D M"                                  # [carbon, cohort] per-plant GPP (period total) (kgC/plant)
# npp_cohort = "D M"                                  # [carbon, cohort] per-plant NPP (period total) (kgC/plant)
# et_site = "D M Y"                                   # [water, site] site evapotranspiration (period total) (kg/m2)
# soil_water_site = "D M Y"                           # [water, soil] area-weighted volumetric soil moisture (m3/m3)
# soil_psi_site = "D M Y"                             # [water, soil] area-weighted soil matric potential (MPa)
# soil_wetness_site = "D M Y"                         # [water, soil] area-weighted relative saturation (-)
# w_surface_site = "D M Y"                            # [water, site] ponded surface water (kg/m2)
# swe_site = "D M Y"                                  # [water, site] snow water equivalent (kg/m2)
# snow_depth_site = "D M Y"                           # [water, site] snow depth (m)
# w_surface_patch = "M"                               # [water, patch] patch ponded surface water (kg/m2)
# swe_patch = "M"                                     # [water, patch] patch snow water equivalent (kg/m2)
# soil_water_layer_patch = "D M"                      # [water, soil,patch] volumetric soil moisture by layer and patch (m3/m3)
# soil_psi_layer_patch = "D M"                        # [water, soil,patch] soil matric potential by layer and patch (MPa)
# soil_wetness_layer_patch = "M"                      # [water, soil,patch] relative saturation by layer and patch (-)
# cas_temp_site = "D M Y"                             # [energy, site] site canopy-air-space temperature (K)
# cas_shv_site = "D M Y"                              # [energy, site] site canopy-air specific humidity (kg/kg)
# cas_co2_site = "D M Y"                              # [energy, site] site canopy-air CO2 mixing ratio (umol/mol)
# cas_vpd_site = "D M Y"                              # [energy, site] site canopy-air vapour-pressure deficit (Pa)
# cas_depth_site = "M Y"                              # [energy, site] site canopy-air-space depth (m)
# soil_temp_top_site = "D M Y"                        # [energy, site] site soil-top (layer 1) temperature (K)
# soil_temp_site = "D M Y"                            # [energy, soil] area-weighted soil temperature (K)
# soil_fliq_site = "D M Y"                            # [energy, soil] area-weighted soil liquid fraction (-)
# leaf_temp_site = "D M Y"                            # [energy, site] leaf-area-weighted canopy leaf temperature (K)
# wood_temp_site = "D M Y"                            # [energy, site] leaf-area-weighted wood temperature (K)
# leaf_temp_cohort = "D M"                            # [energy, cohort] per-cohort leaf temperature (K)
# wood_temp_cohort = "D M"                            # [energy, cohort] per-cohort wood temperature (K)
# cas_temp_patch = "D M"                              # [energy, patch] patch canopy-air-space temperature (K)
# soil_temp_top_patch = "M"                           # [energy, patch] patch soil-top temperature (K)
# soil_temp_layer_patch = "D M"                       # [energy, soil,patch] soil temperature by layer and patch (K)
# soil_fliq_layer_patch = "M"                         # [energy, soil,patch] soil liquid fraction by layer and patch (-)
# soilc_fast_grnd_site = "D M Y"                      # [biogeochem, site] site fast/metabolic litter carbon, above-ground (kgC/m2)
# soilc_fast_soil_site = "D M Y"                      # [biogeochem, site] site fast/metabolic litter carbon, below-ground (kgC/m2)
# soilc_struct_grnd_site = "D M Y"                    # [biogeochem, site] site structural litter + CWD carbon, above-ground (kgC/m2)
# soilc_struct_soil_site = "D M Y"                    # [biogeochem, site] site structural litter + CWD carbon, below-ground (kgC/m2)
# soilc_microbial_site = "D M Y"                      # [biogeochem, site] site microbial SOM carbon (kgC/m2)
# soilc_slow_site = "D M Y"                           # [biogeochem, site] site slow/humified SOM carbon (kgC/m2)
# soilc_passive_site = "D M Y"                        # [biogeochem, site] site passive SOM carbon (kgC/m2)
# soilc_total_site = "D M Y"                          # [biogeochem, site] site total soil organic carbon (kgC/m2)
# rh_site = "D M Y"                                   # [biogeochem, site] site heterotrophic respiration (period total) (kgC/m2)
# soilc_total_patch = "M"                             # [biogeochem, patch] patch total soil organic carbon (kgC/m2)
# rh_patch = "M"                                      # [biogeochem, patch] patch heterotrophic respiration (period total) (kgC/m2)
# work_integ_steps_site = "D M Y"                     # [numerics, site] accepted integrator sub-steps (period total) (--)
# work_integ_rej_site = "D M Y"                       # [numerics, site] rejected integrator steps (period total) (--)
# work_soil_nsub_site = "D M Y"                       # [numerics, site] soil-water solver sub-steps (period total) (--)
# work_hydro_nsub_site = "D M Y"                      # [numerics, site] plant-hydraulics sub-steps (period total) (--)
# work_nonconv_site = "D M Y"                         # [numerics, site] solver non-convergence events (period total) (--)
# work_hydro_thrash_site = "D M Y"                    # [numerics, site] dt_fast steps with pathological hydraulics sub-stepping (period total) (--)
# work_rk45_rescue_site = "D M Y"                     # [numerics, site] RK45 steps rescued to the implicit path (period total) (--)
# work_clamp_stage_site = "D M Y"                     # [numerics, site] stage-input clamp activations (period total) (--)
# work_clamp_commit_site = "D M Y"                    # [numerics, site] committed-state clamp activations (period total) (--)
# work_clamp_mass_site = "D M Y"                      # [numerics, site] water moved by committed-state clamps (period total) (kg/m2)
# work_clamp_energy_site = "D M Y"                    # [numerics, site] energy moved by committed-state clamps (period total) (J/m2)
# anet_cohort = "D M"                                 # [ecophys, cohort] net leaf assimilation (umol/m2 leaf/s)
# agross_cohort = "D M"                               # [ecophys, cohort] gross leaf assimilation (umol/m2 leaf/s)
# gsw_cohort = "D M"                                  # [ecophys, cohort] stomatal conductance to water vapour (mol/m2 leaf/s)
# gbw_cohort = "M"                                    # [ecophys, cohort] leaf boundary-layer conductance (m/s)
# ci_cohort = "D M"                                   # [ecophys, cohort] intercellular CO2 mole fraction (umol/mol)
# cs_cohort = "M"                                     # [ecophys, cohort] leaf-surface CO2 mole fraction (umol/mol)
# rd_cohort = "M"                                     # [ecophys, cohort] leaf dark respiration (umol/m2 leaf/s)
# transp_cohort = "D M"                               # [ecophys, cohort] leaf transpiration (mol/m2 leaf/s)
# beta_stomata_cohort = "D M"                         # [ecophys, cohort] stomatal water-stress factor (-)
# beta_nonstomata_cohort = "D M"                      # [ecophys, cohort] non-stomatal (capacity) water-stress factor (-)
# leaf_vpd_cohort = "M"                               # [ecophys, cohort] leaf-to-air vapour-pressure deficit (Pa)
# psi_leaf_cohort = "D M"                             # [ecophys, cohort] leaf water potential (MPa)
# psi_wood_cohort = "D M"                             # [ecophys, cohort] wood water potential (MPa)
# plc_cohort = "D M"                                  # [ecophys, cohort] plant loss of hydraulic conductance (-)
# sapflow_cohort = "D M"                              # [ecophys, cohort] wood-to-leaf sapflow (kg/plant/s)
# root_uptake_cohort = "D M"                          # [ecophys, cohort] soil-to-root water uptake (kg/plant/s)
# leaf_water_cohort = "M"                             # [ecophys, cohort] internal leaf water (kg/plant)
# wood_water_cohort = "M"                             # [ecophys, cohort] internal wood water (kg/plant)
# abs_par_cohort = "D M"                              # [radiation, cohort] absorbed photosynthetically active radiation (W/m2)
# abs_sw_cohort = "D M"                               # [radiation, cohort] absorbed shortwave radiation (W/m2)
# abs_lw_cohort = "M"                                 # [radiation, cohort] net longwave radiation (W/m2)
# wind_cohort = "M"                                   # [ecophys, cohort] in-canopy wind speed (m/s)
# anet_site = "D M Y"                                 # [ecophys, site] leaf-area-weighted canopy net assimilation (umol/m2 leaf/s)
# gsw_site = "D M Y"                                  # [ecophys, site] leaf-area-weighted canopy stomatal conductance (mol/m2 leaf/s)
# psi_leaf_site = "D M Y"                             # [ecophys, site] leaf-area-weighted canopy leaf water potential (MPa)
# plc_site = "D M Y"                                  # [ecophys, site] leaf-area-weighted canopy loss of conductance (-)
# beta_stomata_site = "D M Y"                         # [ecophys, site] leaf-area-weighted stomatal water-stress factor (-)
# root_uptake_site = "D M Y"                          # [water, site] site root water uptake (kg/m2/s)
# transp_leaf_site = "D M Y"                          # [water, site] leaf-area-weighted canopy transpiration (mol/m2 leaf/s)
# npp_leaf_site = "M Y"                               # [carbon, site] NPP allocated to leaf (kgC/m2/yr)
# npp_fineroot_site = "M Y"                           # [carbon, site] NPP allocated to fine root (kgC/m2/yr)
# npp_wood_site = "M Y"                               # [carbon, site] NPP allocated to wood (kgC/m2/yr)
# npp_storage_site = "M Y"                            # [carbon, site] NPP to non-structural storage (kgC/m2/yr)
# npp_repro_site = "M Y"                              # [carbon, site] NPP allocated to reproduction (kgC/m2/yr)
# growth_resp_site = "M Y"                            # [carbon, site] growth respiration (kgC/m2/yr)
# agb_growth_site = "M Y"                             # [structure, site] site AGB growth rate (kgC/m2/yr)
# agb_mort_site = "M Y"                               # [structure, site] site AGB mortality loss rate (kgC/m2/yr)
# mort_rate_site = "M Y"                              # [structure, site] nplant-weighted mortality rate (1/yr)
# litter_leaf_site = "M Y"                            # [biogeochem, site] leaf litterfall carbon (kgC/m2/yr)
# litter_fineroot_site = "M Y"                        # [biogeochem, site] fine-root litter carbon (kgC/m2/yr)
# litter_struct_site = "M Y"                          # [biogeochem, site] structural litter + CWD carbon (kgC/m2/yr)
# nplant_recruit_site = "M Y"                         # [structure, site] recruitment density flux (plant/m2/yr)
# litter_leaf_patch = "M"                             # [biogeochem, patch] patch leaf litterfall carbon (kgC/m2/yr)
# agb_growth_pft = "M Y"                              # [structure, pft] AGB growth rate by PFT (kgC/m2/yr)
# agb_mort_pft = "M Y"                                # [structure, pft] AGB mortality loss rate by PFT (kgC/m2/yr)
# le_site = "D M Y"                                   # [energy, site] latent heat flux (W/m2)
# h_site = "D M Y"                                    # [energy, site] sensible heat flux (W/m2)
# rnet_site = "D M Y"                                 # [energy, site] net all-wave radiation (W/m2)
# sw_in_site = "D M Y"                                # [energy, site] incident shortwave at canopy top (W/m2)
# sw_ground_site = "D M"                              # [radiation, site] shortwave absorbed at the ground (W/m2)
# lw_ground_site = "D M"                              # [radiation, site] net longwave at the ground (W/m2)
# ustar_site = "D M Y"                                # [energy, site] friction velocity (m/s)
# ggnet_site = "M"                                    # [energy, site] ground conductance (m/s)
# rough_site = "M Y"                                  # [energy, site] aerodynamic roughness length (m)
# displace_site = "M Y"                               # [energy, site] zero-plane displacement height (m)
# ground_temp_site = "D M Y"                          # [energy, site] ground (skin) temperature (K)
# gpp_rate_site = "D M Y"                             # [carbon, site] gross primary productivity (mean rate) (umol/m2/s)
# nee_site = "D M Y"                                  # [carbon, site] net ecosystem exchange (mean rate) (umol/m2/s)
# et_rate_site = "D M Y"                              # [water, site] evapotranspiration (mean rate) (kg/m2/s)
# precip_site = "D M Y"                               # [water, site] total precipitation (mean rate) (kg/m2/s)
# resid_energy_site = "D M Y"                         # [numerics, site] worst whole-column energy-budget residual (W/m2)
# resid_water_site = "D M Y"                          # [numerics, site] worst whole-column water-budget residual (kg/m2/s)
# le_patch = "D M"                                    # [energy, patch] patch latent heat flux (W/m2)
# h_patch = "D M"                                     # [energy, patch] patch sensible heat flux (W/m2)
# rnet_patch = "D M"                                  # [energy, patch] patch net all-wave radiation (W/m2)
# nee_patch = "D M"                                   # [carbon, patch] patch net ecosystem exchange (umol/m2/s)
# cas_temp_fast = "F"                                 # [energy, site] site canopy-air-space temperature (sub-daily) (K)
# soil_temp_top_fast = "F"                            # [energy, site] site soil-top temperature (sub-daily) (K)
# gpp_rate_fast = "F"                                 # [carbon, site] site instantaneous GPP rate (umol/m2/s)
# le_flux_fast = "F"                                  # [water, site] site latent-heat (ET) flux (W/m2)
# h_flux_fast = "F"                                   # [energy, site] site sensible-heat flux (W/m2)
# rnet_fast = "F"                                     # [energy, site] site net all-wave radiation (W/m2)
# sw_in_fast = "F"                                    # [energy, site] incident shortwave at canopy top (W/m2)
# ustar_fast = "F"                                    # [energy, site] friction velocity (m/s)
# air_temp_fast = "F"                                 # [energy, site] reference-level forcing air temperature (K)
# soil_temp_site_fast = "F"                           # [energy, soil] area-weighted soil temperature (sub-daily) (K)
# soil_water_site_fast = "F"                          # [water, soil] area-weighted soil moisture (sub-daily) (m3/m3)
# leaf_temp_cohort_fast = "F"                         # [energy, cohort] per-cohort leaf temperature (sub-daily) (K)
# gpp_cohort_fast = "F"                               # [carbon, cohort] per-cohort GPP rate (sub-daily) (umol/plant/s)
# height_cohort_fast = "F"                            # [energy, cohort] per-cohort height (tallest-cohort selection) (m)
