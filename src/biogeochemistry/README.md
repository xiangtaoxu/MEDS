# biogeochemistry (placeholder)

**Slow, stateful pools**: carbon/nitrogen allocation, litter, soil decomposition, and the like.
Kept separate from `biophysics/` so the fast/slow timescale split (and the netCDF/heavy-dep
boundary) stays clean. Empty until implemented.
