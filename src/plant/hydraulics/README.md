# plant/hydraulics (placeholder)

Per-individual **plant hydraulics** — stateless xylem water-transport / water-stress compute
kernels (the prognostic state, e.g. xylem water potential, lives in the cohort SoA under
`src/state/`, passed by argument — the FATES `*Mem`/compute split). Empty until implemented.
